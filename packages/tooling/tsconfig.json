{
    "$schema": "https://json.schemastore.org/tsconfig",
    "extends": "./src/typescript/config/check.json",

    "compilerOptions": {
        /**
         * Path aliases are disabled because path resolution from `cwd` fails when running the bin scripts from other packages.
         */
        "paths": null
    }
}
