{
    "name": "@altered/core-experimental",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "exports": {
        "./*": {
            "types": "./src/*.ts",
            "bundler": "./src/*.ts",
            "import": "./dist/*.js"
        }
    },
    "scripts": {
        "dev": "tsc -p tsconfig.build.json --watch",

        "check:types": "tsc",

        "clean": "altered-clean-package",

        "build": "tsc -p tsconfig.build.json"
    },
    "devDependencies": {
        "@altered/tooling": "workspace:*",
        "@types/node": "catalog:",
        "typescript": "catalog:"
    },
    "dependencies": {
        "arktype": "catalog:"
    }
}
